machine:
  timezone: Asia/Tokyo
  php:
    version: 5.6.14

dependencies:
  override:
    - composer install --prefer-source --no-interaction

# deployment:
#   master:
#     branch: master
#     commands:
#       - sh deploy.sh

# test:
#   override:
#     - vendor/bin/phpunit
# test:
#   # pre:
#     # - cd Laravel
#   override:
#     - cd Laravel && phpunit
